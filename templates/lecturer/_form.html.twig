{{ form_start(form) }}
 {% if is_granted('ROLE_ADMIN') %}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
     {% else %}
     {{ form_widget(form) }}
     {% endif %}
{{ form_end(form) }}
