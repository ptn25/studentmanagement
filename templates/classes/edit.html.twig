{% form_theme classesForm 'bootstrap_5_layout.html.twig' %}
{% extends "base.html.twig" %}

{% block body %}
    <div class="container col-md-7 text-center mt-3">
        <div class="card p-3 bg-light">
            <h3 class="alert alert-primary">Edit class</h3>
            {{ form_start(classesForm) }}

            {{ form_widget(classesForm) }}
            <input class="btn btn-outline-danger" type="submit" value="Edit">

            {{ form_end(classesForm) }}
        </div>
    </div>
{% endblock body %}
