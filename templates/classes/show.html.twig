{% extends 'base.html.twig' %}

{% block title %}Classes{% endblock %}

{% block body %}
    <h1>Classes</h1>
{% if is_granted('ROLE_ADMIN') %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classes_index') }}">back to list</a>
    
    
    <a href="{{ path('classes_edit', {'id': class.id}) }}">edit</a>

    {{ include('classes/_delete_form.html.twig') }}
    {% else %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class.name }}</td>
            </tr>
             <tr>
                <th>image</th>
                <td>{{ Lecturer.image }}</td>
            </tr>
        </tbody>
        <a href="{{ path('classes_index') }}">back to list</a>
    </table>
    {% endif %}
{% endblock %}
